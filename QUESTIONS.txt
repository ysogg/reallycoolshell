Q1:
From my understanding a pager will change the stdin pipe so that data in stdin isn't taken as input and instead just the keyboard is.

Q2:
cd needs to be built in because it changes the working directory of the shell. If it were to be a program then it would run as a child and not be able to modify the directory of the parent.

Q3:
Microsoft recommends the C/C++ debugger and the CRT heap functions to catch memory leaks. On Mac you can use the leaks command on a running program to check for memory leaks similar to Valgrind.


